{
  "providers": [
    {
      "name": "OpenAI",
      "type": "openai",
      "baseURL": "https://api.openai.com/v1",
      "apiKey": "YOUR_OPENAI_API_KEY",
      "availableModels": [
        "o3-mini",
        "o1",
        "gpt-4.5-preview"
      ]
    },
    {
      "name": "Ollama",
      "type": "openai",
      "baseURL": "http://localhost:11434/v1",
      "apiKey": "ollama",
      "availableModels": [
        "smollm:135m",
        "llama3.2:1b",
        "gpt-oss:120b-cloud",
        "qwen3-coder:480b-cloud",
        "glm-4.6:cloud",
        "deepseek-r1:8b",
        "deepseek-v3.1:671b-cloud",
        "gemma3:4b-it-qat",
        "llama3.2:latest",
        "nomic-embed-text:latest"
      ]
    },
    {
      "name": "Anthropic",
      "type": "anthropic",
      "baseURL": "https://api.anthropic.com/v1",
      "apiKey": "YOUR_CLAUDE_API_KEY",
      "availableModels": [
        "claude-4.5-opus-20251124",
        "claude-4.5-sonnet-20250929",
        "claude-4.5-haiku-20251015"
      ]
    },
    {
      "name": "xAI",
      "type": "openai",
      "baseURL": "https://api.grok.ai/v1",
      "apiKey": "YOUR_GROK_API_KEY",
      "availableModels": ["grok-4", "grok-4-mini", "grok-3"]
    },
    {
      "name": "Google",
      "type": "google",
      "baseURL": "https://generativelanguage.googleapis.com/v1beta/openai",
      "apiKey": "YOUR_GEMINI_API_KEY",
      "availableModels": ["gemini-3-pro", "gemini-3-flash", "gemini-2.0-flash"]
    }
  ],
  "promptOverrides": [
    {
      "model": "deepseek-r1:8b",
      "html": {
        "system": "You are a specialized HTML generation API. Output ONLY HTML code in a single code block. You may include text before and after the code block, but ensure there is exactly one code block with ```html or ``` at the start. HTML must be a complete HTML5 document. NO <style> or <script> tags inside HTML. NO local files. NO thinking tags."
      },
      "css": {
        "system": "You are a specialized CSS generation API. Output ONLY CSS code in a single code block. You will be provided with the HTML code. Use it to ensure styles match. You may include text before and after the code block, but ensure there is exactly one code block with ```css or ``` at the start. Only raw CSS code. NO <style> tags."
      },
      "js": {
        "system": "You are a specialized JavaScript generation API. Output ONLY JavaScript code in a single code block. You will be provided with the HTML and CSS code. Use them to ensure functionality matches. You may include text before and after the code block, but ensure there is exactly one code block with ```js or ```javascript or ``` at the start. Only raw JS code. NO <script> tags."
      }
    }
  ],
  "prompts": {
    "html": {
      "system": "You are a specialized HTML generation API. Your ONLY job is to output HTML code for the requested UI. Output the HTML in a single code block. You may include explanatory text before and after the code block if needed, but ensure there is exactly one code block starting with ```html or ```.\n\nSTRICT RULES:\n- HTML: Must be a complete HTML5 document. NO <style> or <script> tags inside HTML. NO local file links (href=\"style.css\", src=\"script.js\"). Use CDNs if absolutely needed.\n- Output format: ```html\n   <!DOCTYPE html>\n   <html>\n   <head>\n     <meta charset=\"UTF-8\" />\n     <title>Generated UI</title>\n   </head>\n   <body>\n     <!-- Your HTML Content Here -->\n   </body>\n   </html>\n   ```"
    },
    "css": {
      "system": "You are a specialized CSS generation API. Your ONLY job is to output CSS code for styling the requested UI. You will be provided with the HTML code generated for this UI. Use the classes and IDs from the provided HTML to ensure the styles match perfectly. Output the CSS in a single code block. You may include explanatory text before and after the code block if needed, but ensure there is exactly one code block starting with ```css or ```.\n\nSTRICT RULES:\n- CSS: Only raw CSS code. NO <style> tags.\n- Output format: ```css\n   /* Your CSS Content Here */\n   ```"
    },
    "js": {
      "system": "You are a specialized JavaScript generation API. Your ONLY job is to output JavaScript code for the requested UI functionality. You will be provided with the HTML and CSS code generated for this UI. Use the IDs and classes from the provided HTML to attach event listeners and manipulate the DOM. Output the JavaScript in a single code block. You may include explanatory text before and after the code block if needed, but ensure there is exactly one code block starting with ```js or ```javascript or ```.\n\nSTRICT RULES:\n- JS: Only raw JS code. NO <script> tags.\n- Output format: ```js\n   /* Your JavaScript Content Here */\n   ```"
    }
  }
}

